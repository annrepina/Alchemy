#pragma once
#include "Alchemist.h"
#include <fstream>

class AlchemistBinaryReader
{
public: 

	//// Конструктор
	//AlchemistBinaryReader(string path);

	Alchemist* readFromFile(string path);

private:

	//string path;
};

